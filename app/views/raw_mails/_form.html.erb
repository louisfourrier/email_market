<%= form_for(@raw_mail) do |f| %>
  <% if @raw_mail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raw_mail.errors.count, "error") %> prohibited this raw_mail from being saved:</h2>

      <ul>
      <% @raw_mail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email_from %><br>
    <%= f.text_area :email_from %>
  </div>
  <div class="field">
    <%= f.label :email_body %><br>
    <%= f.text_area :email_body %>
  </div>
  <div class="field">
    <%= f.label :email_subject %><br>
    <%= f.text_area :email_subject %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
